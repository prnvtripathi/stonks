# Correctness and publication remediation progress

| task | finding IDs | base | head | focused checks | review round | open findings | status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| R01: validate the candidate and reconcile actual source coverage | F01, F02 | `1469b1c` | `9f102c3` | `.venv/bin/python -m pytest pipeline/tests/integration/test_candidate_gate.py pipeline/tests/integration/test_cli.py pipeline/tests/integration/test_publication.py pipeline/tests/validation/test_reconcile.py -q` (59 passed); `.venv/bin/python -m pytest pipeline/tests -q` (209 passed); `git diff --check` | 2 review/fix rounds | Deferred Minor: source-calendar observation (named AMFI market-holiday calendar remains future work) | DONE_WITH_DEFERRED_MINOR |
| R02: fingerprint every contributing input and preserve lineage | F03 | `82fd0c1` | `e935649` | `.venv/bin/python -m pytest pipeline/tests/integration/test_input_identity.py pipeline/tests/integration/test_backfill.py -q` (13 passed); `.venv/bin/python -m pytest pipeline/tests -q` (213 passed); `git diff --check` | fix round 1 review complete | None identified | DONE |
| R03: correct published risk window and MF benchmark semantics | F04, F05 | `0e782c0` | `afb8c6e` | `.venv/bin/python -m pytest pipeline/tests/analytics pipeline/tests/integration/test_end_to_end_publication.py pipeline/tests/integration/test_publication.py pipeline/tests/integration/test_input_identity.py -q` (55 passed); `.venv/bin/python -m pytest pipeline/tests -q` (216 passed); `pnpm --dir packages/contracts test` (8 passed); `pnpm --dir packages/contracts typecheck`; `git diff --check` | self-review complete | Reviewed official MF benchmark mappings and aligned observations are not wired; benchmark RS is deliberately missing with an explicit reason | DONE |
| R04: preserve compact-export momentum metadata at the Worker boundary | F06 | `176c33e` | `87b86ff` | `pnpm --filter @stonks/api exec vitest run src/publication-contract.test.ts` (3 passed); `pnpm --filter @stonks/api exec vitest run` (33 passed); `pnpm --filter @stonks/api exec tsc --noEmit`; `pnpm --filter @stonks/query exec vitest run` (13 passed); `.venv/bin/python3 -m pytest pipeline/tests/integration/test_publication.py -q` (21 passed); direct compact-fixture validation; `git diff --check` | initial review + 1 correction/re-review | None identified | DONE |
| R05: make saved runs immutable historical records | F07 | `199a374` | `6d6ea9f` | `pnpm --filter @stonks/api test` (37 passed); `pnpm --filter @stonks/web test` (25 passed); `pnpm --filter @stonks/contracts test` (8 passed); `pnpm --filter @stonks/query test` (13 passed); `pnpm --recursive --if-present typecheck`; `.venv/bin/pytest -q pipeline/tests/integration/test_publication.py` (21 passed); `git diff --check` | self-review + fix rounds 1–2 complete | R06 still owns bounding D1 result/run retrieval; R05 preserves the existing bounded response slice and removes historical dependence on the current projection. | DONE |
| R06: bound saved-screen execution and result retrieval in D1 | F08 | `3138a91` | `b4662d3` | `pnpm --filter @stonks/api exec vitest run src/result-pagination.test.ts` (17 passed); `pnpm --filter @stonks/api exec vitest run` (54 passed); `pnpm --filter @stonks/api exec tsc --noEmit`; `pnpm --filter @stonks/web test` (25 passed); `pnpm --filter @stonks/contracts test` (8 passed); `pnpm --filter @stonks/query test` (13 passed); `pnpm --recursive --if-present typecheck`; `.venv/bin/python -m pytest pipeline/tests -q` (216 passed); `git diff --check` | self-review + 1 review/fix round complete | None identified | DONE |
| R07: version history partitions and define derived-data retention | F14 | `24b3c73` | `770c869` | `.venv/bin/python -m pytest pipeline/tests/integration/test_history_store.py pipeline/tests/integration/test_publication_bundle.py pipeline/tests/integration/test_publication.py pipeline/tests/integration/test_end_to_end_publication.py pipeline/tests/integration/test_cloudflare_publication.py pipeline/tests/integration/test_budgets.py -q` (49 passed); `.venv/bin/python -m pytest pipeline/tests -q` (228 passed, 2 pre-existing failures in `test_r2_sync.py` -- untracked R08-scoped module explicitly noted in the plan as needing to adapt to R07's bundle; not modified here); `.venv/bin/python -m mypy market_pipeline/publication/bundle.py market_pipeline/storage/history_store.py market_pipeline/publication/d1_export.py market_pipeline/jobs/publish.py` (0 new errors; 4 pre-existing unrelated errors at publish.py:374-376 outside this diff); `git diff --check` | self-review complete | `test_r2_sync.py`'s two tests fail against the new content-hashed `history_key` signature by design -- R08 owns adapting its fixed-key assumptions to this bundle, per the plan's own note that this file "belongs to R08" | DONE |
| R08: finish one bounded R2 transport and make failure propagation explicit | F09, F10 | `9f0ecae` | `121db5f` | `.venv/bin/python -m pytest pipeline/tests/integration/test_r2_sync.py pipeline/tests/integration/test_cloudflare_publication.py -q` (14 passed); `.venv/bin/python -m pytest pipeline/tests -q` (238 passed); `.venv/bin/python -m mypy pipeline` (0 new errors; 7 pre-existing unrelated errors in `jobs/publish.py:374-376` and `tests/integration/test_history_store.py:29,86,88`, untouched by this diff); `.venv/bin/python -m ruff check pipeline/market_pipeline/publication/r2_sync.py pipeline/tests/integration/test_r2_sync.py` (clean); `uv add boto3` (network available; resolved cleanly, no lockfile hand-editing); `git diff --check` | self-review complete | None identified. `r2_sync.py`'s `synchronize_history(bundle, history_root, client, ...)` now consumes R07's `PublicationBundle` directly (no more mutable/immutable manifest); the workflow's R2 upload step is one `r2_sync` CLI invocation whose exit code gates the D1 import step, replacing the process-substitution loop (F09/F10); added `R2_ACCESS_KEY_ID`/`R2_SECRET_ACCESS_KEY` as a job secret pair distinct from `CLOUDFLARE_API_TOKEN`, documented in `docs/operations/security-checklist.md` section 7 | DONE |
| R09: validate capacity telemetry and budget the whole publication attempt | F11, F14 | `d4b10e5` | `8ace42b` | `.venv/bin/python -m pytest pipeline/tests/integration/test_remote_usage.py pipeline/tests/integration/test_publication.py pipeline/tests/integration/test_cloudflare_publication.py pipeline/tests/integration/test_budgets.py -q` (67 passed); `.venv/bin/python -m pytest pipeline/tests -q` (273 passed); `.venv/bin/python -m mypy market_pipeline/publication/preflight.py market_pipeline/publication/d1_export.py market_pipeline/publication/remote_usage.py` (clean); `.venv/bin/python -m mypy market_pipeline` (0 new errors; same 4 pre-existing unrelated errors at `jobs/publish.py:374-376`); `.venv/bin/python -m ruff check pipeline/market_pipeline/publication/preflight.py pipeline/market_pipeline/publication/d1_export.py pipeline/market_pipeline/publication/remote_usage.py pipeline/tests/integration/test_remote_usage.py pipeline/tests/integration/test_cloudflare_publication.py` (clean); `git diff --check` | self-review complete | A prior, interrupted agent had already built and tested `remote_usage.py`/`test_remote_usage.py` (32 passing tests) but left them uncommitted and unwired; this task wired them into `preflight.py` (new `assert_plan_within_remote_budget`/`evaluate_publication_attempt`, additive alongside the untouched `assert_plan_within_free_tier`) and `d1_export.py` (explicit `weekday_runs_per_month` override, defaulting unchanged), added the workflow's GraphQL Analytics telemetry step and reservation-ledger recording step (both reasoned through, not executed against a live account), added job/step `timeout-minutes`, and documented `Account Analytics Read`/`CLOUDFLARE_D1_DATABASE_ID`. Known limitation: the reservation ledger records the resolved plan's own conservative counts rather than r2_sync's true attempted-request count, since r2_sync's CLI does not expose a class-A/B split on stdout today (out of scope: r2_sync.py belongs to R08) | DONE |
| R10: restore durable source/checkpoint state on a clean runner | F12 | `d9b8fe5` | `af1e24d` | `.venv/bin/python -m pytest pipeline/tests/integration/test_checkpoint_restore.py pipeline/tests/integration/test_checkpoint_archive_workflow.py pipeline/tests/integration/test_r2_sync.py pipeline/tests/integration/test_cloudflare_publication.py -q` (28 passed); `.venv/bin/python -m pytest pipeline/tests -q` (291 passed); `.venv/bin/python -m mypy pipeline/market_pipeline` (0 new errors; same 4 pre-existing unrelated errors at `jobs/publish.py:374-376`); `.venv/bin/python -m ruff check pipeline/market_pipeline/publication/checkpoint_archive.py pipeline/market_pipeline/publication/r2_sync.py pipeline/tests/integration/test_checkpoint_restore.py pipeline/tests/integration/test_checkpoint_archive_workflow.py pipeline/tests/integration/test_r2_sync.py` (clean); `git diff --check` | 1 external review round, 1 fix round complete | New `publication/checkpoint_archive.py` builds a versioned checkpoint archive (SQLite backup via the backup API, immutable raw triples reusing `raw_store.py`'s own commit-marker integrity check, and a content-addressed manifest) with a private `state/latest-success.json` pointer advanced only by `promote_latest_success` after an independently remote-verified active dataset ID; reuses R08's `r2_sync.py` transport via two new small public functions (`put_and_verify_object`/`get_and_verify_object`, plus a `build_s3_client` factory, now wrapped in `NoSuchKeyTranslatingClient`) rather than a second S3 client. Wired into `daily-data.yml`: a restore-or-identity-check step after the existing `actions/cache` restore (forcing a real restore on a detected mismatch, not just a warning), and a backup+promote step only after "Verify remote active dataset" succeeds, both folding their put/get counts into R09's reservation ledger. Fix round 1 (see task-R10-report.md's appended section) closed a Critical finding (raw-triple restore is now staged-then-atomically-swapped, so a mid-loop failure can never leave a partial `raw/` for a later cache save to trust) and three Important findings (forced re-restore on mismatch, real botocore `ClientError`->`KeyError` translation, corrected restore-atomicity documentation) | DONE |
| R11: restore market snapshots instead of only switching a pointer | F13 | `49f304e` | `595e5f6` | `.venv/bin/python -m pytest pipeline/tests/integration/test_remote_restore.py pipeline/tests/integration/test_publication.py pipeline/tests/integration/test_end_to_end_publication.py pipeline/tests/integration/test_cloudflare_publication.py pipeline/tests/integration/test_r2_sync.py pipeline/tests/integration/test_checkpoint_restore.py pipeline/tests/integration/test_checkpoint_archive_workflow.py -q` (62 passed); `.venv/bin/python -m pytest pipeline/tests -q` (299 passed); `.venv/bin/python -m mypy market_pipeline` (0 new errors; same 4 pre-existing unrelated errors at `jobs/publish.py:374-376`); `.venv/bin/python -m ruff check market_pipeline/publication/restore.py market_pipeline/publication/d1_export.py tests/integration/test_remote_restore.py` (clean); `git diff --check` | self-review complete | New `publication/restore.py` selects an explicit, currently-*retained* dataset (R07's `reachable_bundles`: active + previous successful promotion only), requires its recorded bundle to carry a real `input_manifest_hash` and at least one object, re-verifies every object against R08's own bounded/checksum-verified R2 transport (`r2_sync.synchronize_history` -- no second S3 client), and only then reimports its compact projection through a new `d1_export.export_dataset` (an explicit-`dataset_id` generalization of `export_active_dataset`, refactored to share one statement-building body `_export_dataset_sql` so both entry points stay byte-identical in shape/scope -- market-data tables only, `saved_screens`/`screen_runs`/`screen_matches` never touched). Every check happens before `export_dataset` is even called, so a rejected dataset, a corrupt local object, or a failed/corrupted remote object all fail closed before any SQL is generated or `connection` is read further -- verified by dedicated fail-closed tests. Added a production-schema regression harness (`D1Publisher.initialize_schema()` against an in-memory SQLite standing in for D1, exercising the real 0001-0008 migrations including `saved_screens`/`screen_runs`) reproducing pointer-only rollback returning zero `instrument_snapshots` rows, then a full round-trip (import A, saved screen + run, import B, edit screen + new run, restore A) proving A's projection returns while both B-era product-state edits survive untouched. Rewrote `docs/operations/recovery.md` section 2 with the `restore.py` CLI, the existing daily-workflow preflight/D1-apply/verify steps restore reuses unmodified, and an explicit note that this pipeline's local-only `sqlite3.executescript` tests cannot themselves prove D1's own remote import-transaction rollback (a live-account acceptance drill remains a separate, undone step) -- while keeping a documented local-only pointer-flip fallback for `market.db`, which never itself serves reads from `instrument_snapshots`. Deliberate deviation from the brief's literal "verifies its SQL ... checksum" wording: a dataset's locally-recorded `PublicationBundle.sql_checksum` is never persisted back to the local database by the existing R07/R09 export path (it is only ever written into a same-run object-manifest artifact), so restore does not compare against a stale local value; it computes and records its own fresh SQL checksum for the restore's own generated import, exactly as a normal export does. | DONE |
| R12: make deployment authentication and privacy gates truthful | F15 | `245b912` | (this task's commit) | `pnpm --filter @stonks/api exec vitest run src/middleware/access.test.ts` (7 passed); `pnpm --filter @stonks/api exec vitest run e2e/assert-anonymous-denied.test.ts` (9 passed); `pnpm --filter @stonks/api exec vitest run` (68 passed); `pnpm --filter @stonks/api exec tsc --noEmit`; `pnpm typecheck` (all workspaces); `npx eslint apps/api/src/middleware/access.ts apps/api/src/middleware/access.test.ts apps/api/src/env.ts apps/api/src/index.ts apps/api/e2e/assert-anonymous-denied.test.ts` (clean; repo-wide `pnpm lint` has 2 pre-existing failures in `.venv` and `result-pagination.test.ts`, neither touched here); `python3 -c "import yaml; ..."` on both changed workflow files (parse OK); `pnpm --filter @stonks/web test:e2e` (2 passed, both browser journeys); manual `bash` exercises of the hardened `Resolve run parameters` script proving a shell-metacharacter payload in `date` is rejected by format validation rather than executed, and that `manifest_path` traversal/`previous_count` injection attempts are also rejected | self-review complete | Added a scoped Cloudflare Access service-token identity (`common_name` claim, separate `ACCESS_ALLOWED_SERVICE_TOKENS` allowlist in `apps/api/src/env.ts`) authorized only for `GET /api/v1/status`, sharing one `authorizeIdentity` helper between the production RS256 verifier and the HS256 test double so the two paths cannot drift; a matched service identity used for a mutation or any other route returns 403 (never 401, never treated as an owner email). Hardened `scripts/ci/assert-anonymous-denied.sh` to require genuine denial evidence (401, 403, or a redirect to Access's hosted login page) rather than treating any non-200 (including 5xx, 404, or a transport failure) as "denied" -- covered by 9 tests in `apps/api/e2e/assert-anonymous-denied.test.ts` against local HTTP fixtures (including a closed-port connection-refused case and a real 1-second-timeout case). Updated both `deploy.yml` smoke-test steps to require a strict `200` plus `jq`-validated status-JSON shape (`datasetId`/`sources` present; a bootstrap `datasetId: null` is accepted as healthy-empty, per the brief -- a real dataset ID is a separate, not-yet-existing acceptance gate). Moved `daily-data.yml`'s six free-form `workflow_dispatch` string inputs through a step `env:` block instead of direct `${{ inputs.X }}` script-text interpolation, added format validation (date regex, numeric range, manifest-path traversal guard) before their first use, and did the same for the one downstream step that re-interpolated the same values from `steps.params.outputs.*` (defense in depth, same file, same root values). Concurrency: replaced `deploy.yml`'s single workflow-level group and `daily-data.yml`'s independent `daily-data` group with three job-level groups -- `deploy-preview` (preview only, unchanged serialization against itself), and a `production-d1-writer` group shared by `deploy.yml`'s `production` job and `daily-data.yml`'s `refresh` job (both mutate the production D1 database and were previously not mutually exclusive). Chose job-level over a single shared workflow-level group specifically so `preview` deploys are not needlessly delayed behind an unrelated daily data refresh. **Live-acceptance gate still pending** (documented in `docs/operations/deploy.md`): a real Cloudflare Access Service Token against a real deployed Worker was not and could not be exercised from this environment; only the verification logic, the hardened denial script, and the workflow YAML were implemented and unit/local-fixture tested. | DONE |
| R13: stop embedding the full input manifest in `datasets.metadata_json` | F16 (new, found by the phase acceptance gate) | `d556e07` | (this task's commit) | `.venv/bin/python -m pytest pipeline/tests/integration/test_end_to_end_publication.py pipeline/tests/integration/test_input_identity.py pipeline/tests/integration/test_publication.py pipeline/tests/integration/test_publication_bundle.py pipeline/tests/integration/test_history_store.py pipeline/tests/integration/test_checkpoint_restore.py pipeline/tests/integration/test_remote_restore.py pipeline/tests/integration/test_backfill.py pipeline/tests/integration/test_cli.py -q` (80 passed); `.venv/bin/python -m pytest pipeline/tests -q` (300 passed); `.venv/bin/python -m mypy pipeline/market_pipeline` (0 new errors; same 4 pre-existing unrelated errors, now at `jobs/publish.py:380-382` after this diff shifted line numbers -- same code, untouched by this change); `.venv/bin/python -m ruff check pipeline/market_pipeline/jobs/publish.py pipeline/market_pipeline/storage/history_store.py pipeline/tests/integration/test_end_to_end_publication.py pipeline/tests/integration/test_input_identity.py` (clean); `git diff --check` | self-review complete | None identified | DONE |
| Phase acceptance gate (verification + documentation, run once after R01-R12) | F01-F15 | `fecf313` | (this task's commit) | `.venv/bin/python -m pytest pipeline/tests -q` (299 passed, after fixing a pre-existing 3-test failure exposed only by running the full suite -- see below); `.venv/bin/python -m ruff check pipeline` (clean, after autofixing 3 pre-existing import-order nits); `.venv/bin/python -m mypy pipeline/market_pipeline` (0 new errors; same 4 pre-existing errors at `jobs/publish.py:374-376`); `.venv/bin/python -m mypy pipeline` (15 errors, all pre-existing across `test_history_store.py`/`test_remote_usage.py`/`test_r2_sync.py`/`test_checkpoint_restore.py`/`jobs/publish.py`, none newly introduced); `pnpm --recursive test` (all workspaces green: contracts 8, query 13, api 68, web 25); `pnpm --recursive typecheck` (clean); `pnpm lint` (2 pre-existing failures in `.venv`'s vendored JS and `result-pagination.test.ts`, both already documented as pre-existing by R12, neither touched); `pnpm --filter @stonks/web test:e2e` (2 passed); `pnpm --filter @stonks/web build`; `wrangler deploy --dry-run` for `--env preview` and `--env production` (both succeed, packaging evidence only, not a live deploy) | self-review complete | Found and fixed one real composition-only test regression (R12's workflow comment collided with R10's fragile step-boundary string search in `test_checkpoint_archive_workflow.py`; fixed by rewording the comment, no behavior change). Found and reported, but deliberately did not fix, one real pre-existing defect surfaced only by this gate's cross-boundary fixture: R02's full input-manifest embedding in `datasets.metadata_json` can exceed `d1_export.py`'s pre-existing 90,000-byte per-statement D1 export limit once artifact count is in the hundreds -- a real three-year AMFI backfill (~750+ daily artifacts) is expected to trip this on every export. Documented as a blocking pre-launch defect in `docs/operations/launch-checklist.md` rather than silently worked around. Chained-scenario requirement satisfied via a documented composition argument (route (b): citing each of R01/R02/R08/R10/R11's existing tests per stage) rather than a new combined integration test -- justified in `docs/operations/launch-checklist.md` and the phase-gate report. Capacity counts aggregated from existing R06/R08/R09/R10 measurements with no gap found in retries/archive/retention coverage. Live preview exercise with real Cloudflare bindings explicitly out of scope for this environment and recorded as a pending external dependency. | DONE_WITH_CONCERNS |
| S01: define and enforce supplied-file versus network acquisition | F16 | `f31bac6` | `e714f66` | `.venv/bin/python -m pytest pipeline/tests/integration/test_supplied_source_admission.py -q` (20 passed); `.venv/bin/python -m pytest pipeline/tests/sources pipeline/tests/storage pipeline/tests/integration/test_cli.py -q` (67 passed); `.venv/bin/python -m pytest pipeline/tests -q` (320 passed); `.venv/bin/python -m mypy pipeline/market_pipeline` (0 new errors; same 4 pre-existing errors, now at `jobs/publish.py:380-382` after this diff shifted line numbers); `.venv/bin/python -m ruff check pipeline/market_pipeline/domain/models.py pipeline/market_pipeline/sources/registry.py pipeline/market_pipeline/sources/__init__.py pipeline/market_pipeline/storage/raw_store.py pipeline/market_pipeline/jobs/source_inputs.py pipeline/tests/integration/test_supplied_source_admission.py` (clean); `git diff --check` | self-review + 1 review/fix round complete | Fixed one Important finding: `admit()` trusted a caller-supplied policy outright for any source ID absent from `SOURCE_POLICIES`; added an explicit, default-`False` `allow_unregistered_source` opt-in so an unregistered source is rejected by `admit()` itself again (matching the old `assert_artifact_policy` invariant), with a dedicated regression test (see task-S01-report.md fix round 1) | DONE |
| S02: publish equity/ETF observations and corporate-action-adjusted analytics | F16 | `e714f66` | `b8de9bf` | `.venv/bin/python -m pytest pipeline/tests/integration/test_nse_candidate.py -q` (15 passed); `.venv/bin/python -m pytest pipeline/tests/normalization pipeline/tests/analytics pipeline/tests/sources pipeline/tests/storage pipeline/tests/integration pipeline/tests/domain -q` (307 passed); `.venv/bin/python -m pytest pipeline/tests -q` (335 passed); `.venv/bin/python -m mypy pipeline/market_pipeline/jobs/nse_candidate.py` (clean); `.venv/bin/python -m mypy pipeline/market_pipeline` (0 new errors; same 4 pre-existing errors at `jobs/publish.py:380-382`, untouched by this diff); `.venv/bin/python -m ruff check pipeline/market_pipeline/jobs/nse_candidate.py pipeline/tests/integration/test_nse_candidate.py` (clean); `git diff --check` | self-review complete | None identified | DONE |
